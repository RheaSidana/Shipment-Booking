import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  top:{
    marginTop: "125px",
    fontWeight: "bolder"
  },
  flexing:{
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
    fontFamily: "Figtree",
    fontWeight: "bold",
  }
}));
